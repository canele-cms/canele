{
  "type": "module",
  "packageManager": "pnpm@9.14.2",
  "scripts": {
    "dev": "pnpm -F dash build && pnpm -F astro sync && pnpm -F dev -F dash dev",
    "check": "pnpm biome check",
    "build": "pnpm -F \"@canele-cms/*\" --parallel build"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4"
  }
}
