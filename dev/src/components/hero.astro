---
import { text, type ComponentProps } from "@canele-cms/astro/schema";

export const fields = {
  title: text({ required: true }),
};

type Props = ComponentProps<typeof fields>;

const { title } = Astro.props;
---

<section>
  <h2>{title}</h2>
</section>
